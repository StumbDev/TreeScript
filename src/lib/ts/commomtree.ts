import JSON5 from "json5";

const reqFiles = "ctsconfig.json";
const mainFIle = "main.voi";
const jsFile = "commontree.js";

export function CommonTree() {
    // make the CommonTree thing
}

export function jsonInterpret() {
    // interpret the ctsconfig.json file
    /*
    
    */
    
}